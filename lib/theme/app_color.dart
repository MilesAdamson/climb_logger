import 'package:flutter/material.dart';

class AppColor {
  AppColor._();

  static const colorOneDefaultValue = Colors.red;
  static const colorTwoDefaultValue = Colors.yellow;
  static const colorThreeDefaultValue = Colors.orange;
  static const colorFourDefaultValue = Colors.pinkAccent;
  static const colorFiveDefaultValue = Colors.lightBlueAccent;
  static const colorSixDefaultValue = Colors.green;
  static const darkBg = Color(0xFF1f1f1f);
  static const unknownColor = Color(0xFF000001);
}
