import 'package:flutter/material.dart';

const kListViewPadding = EdgeInsets.only(top: 8.0, bottom: 100.0);

const kCompletedItemOpacity = 0.6;

double bottomSheetHeight(BuildContext context) =>
    MediaQuery.of(context).size.height * 0.89;

const kCardOutsidePadding = EdgeInsets.all(4.0);

const kTimerAnimationDuration = Duration(milliseconds: 500);

final kTableBorder = TableBorder.all(color: Colors.white70);
